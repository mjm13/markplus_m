# 拖拽功能使用说明

## 功能概述
新增的拖拽功能允许用户通过拖拽操作将右侧书签列表中的书签移动到左侧文件夹树中的任意文件夹。

## 使用方法

### 1. 启用拖拽功能
- 确保处于**编辑模式**（右上角开关切换到 "E" 状态）
- 只有在编辑模式下才能进行拖拽操作

### 2. 执行拖拽操作
1. 在右侧书签列表中，找到要移动的书签
2. 按住鼠标左键开始拖拽书签
3. 将书签拖拽到左侧文件夹树中的目标文件夹上
4. 当文件夹高亮显示时，释放鼠标完成移动

### 3. 视觉反馈
- **拖拽开始**: 被拖拽的书签会变为半透明状态
- **悬停文件夹**: 目标文件夹会显示蓝色边框和高亮效果
- **拖拽结束**: 显示操作结果提示信息

## 技术实现

### 核心技术
- **HTML5 Drag and Drop API**: 原生拖拽支持
- **Vue 3 事件处理**: 响应式拖拽状态管理
- **Chrome Bookmarks API**: 同步浏览器书签结构

### 关键组件
1. **拖拽源** (右侧书签列表)
   - `draggable="true"` 属性启用拖拽
   - `@dragstart` 事件设置拖拽数据
   - `@dragend` 事件清理拖拽状态

2. **拖拽目标** (左侧文件夹树)
   - `@dragover` 事件允许放置
   - `@drop` 事件处理放置逻辑
   - `@dragenter/@dragleave` 事件管理视觉反馈

### 数据流程
```
用户开始拖拽
    ↓
设置拖拽数据 (JSON格式)
    ↓
悬停目标文件夹 (高亮显示)
    ↓
释放到目标文件夹
    ↓
解析拖拽数据
    ↓
更新书签父级ID
    ↓
同步Chrome书签
    ↓
刷新界面显示
```

## 错误处理

### 常见情况处理
- **重复移动**: 检测书签是否已在目标文件夹中
- **书签不存在**: 验证拖拽的书签是否有效
- **移动失败**: 显示错误提示并回滚操作

### 国际化支持
- 中文: "移动成功", "书签已在该文件夹中", "移动失败"
- 英文: "Move successful", "Bookmark is already in this folder", "Move failed"

## 样式说明

### CSS 类
- `.dragging`: 拖拽中的书签样式（半透明 + 轻微旋转）
- `.drag-over`: 悬停文件夹的高亮样式
- `.folder-drop-zone`: 文件夹拖拽区域基础样式

### 动画效果
- 拖拽状态切换使用 `transition: all 0.2s ease`
- 文件夹高亮使用渐变边框效果

## 兼容性
- 支持所有现代浏览器的 HTML5 拖拽 API
- 与现有的 Chrome 扩展权限兼容
- 不影响原有的书签管理功能

## 注意事项
1. 只能在编辑模式下使用拖拽功能
2. 只能将书签拖拽到文件夹中，不能拖拽文件夹
3. 拖拽操作会自动同步到 Chrome 浏览器的书签
4. 移动后会自动刷新界面显示最新状态